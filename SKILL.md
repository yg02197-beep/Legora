# Legora Skill Compatibility Pointer

The canonical Agent Skill is `skills/legora/SKILL.md`.

Manual or pre-R5 consumers should load that file and its colocated `references/` directory. This root file is only a compatibility pointer and is not an independent workflow authority.
